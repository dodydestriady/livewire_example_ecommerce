<div>
    <div class="card">
        <div class="card-header">
            <span class="card-title">{{ __('Transaction') }}</span>
        </div>
        <div class="card-body">
            <!-- @if (session()->has('message'))
                <div class="alert alert-success">
                    {{ session('message') }}
                </div>
            @endif
            <livewire:account.address-form></livewire:account.address-form>
            <table class="table mt-3">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>{{ __('Name') }}</th>
                        <th>{{ __('Address') }}</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table> -->
        </div>
    </div>
</div>